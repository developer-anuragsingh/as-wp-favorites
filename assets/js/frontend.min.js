jQuery( document ).ready( function ( e ) {
	jQuery("#form-favorite").submit(function(event){
		event.preventDefault();


		//alert( "Handler for .submit() called." );

		var id_user = jQuery("#id_loged_in_user").val();
		var id_favorite = jQuery("#id_to_add_in_favorite").val();

		// var asUrl =  myAjax.url;

		// alert(asUrl);
		
		jQuery.ajax({
			url 	: myAjax.url,
			type	: 'POST',
			dataType: 'json',
			data	: {
						action	: 'add_to_my_favorite',
						id_user : id_user,
						id_favorite : id_favorite
					}
			,beforeSend : function (response) {
				console.log("Sending");
			}
			,success : function (response) {
				console.log("send");
				console.log(response);
			}
			,error: function(response){
		        console.log(response); // error
		    }

		});
	});
});