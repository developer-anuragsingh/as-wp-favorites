jQuery( document ).ready( function ( e ) {
	jQuery("#form-favorites").submit(function(event){
		event.preventDefault();


		//alert( "Handler for .submit() called." );

		var id_user = jQuery("#id_loged_in_user").val();
		var id_favorites = jQuery("#id_to_add_in_favorites").val();

		// var asUrl =  myAjax.url;

		// alert(asUrl);
		
		jQuery.ajax({
			url 	: myAjax.url,
			type	: 'POST',
			dataType: 'json',
			data	: {
						action	: 'add_to_my_favorites',
						id_user : id_user,
						id_favorites : id_favorites
					}
			,beforeSend : function (response) {
				console.log("Sending");
			}
			,success : function (response) {
				console.log("send");
				console.log(response);
			}
			,error: function(response){
		        console.log(response); // error
		    }

		});
	});
});